name: SKATEBOARD_FORGE
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build_artifacts:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
      - name: 🛠️ Install Build Tools
        run: sudo apt-get update && sudo apt-get install -y mingw-w64 libsdl2-dev libgl1-mesa-dev libglu1-mesa-dev
      - name: 📥 Fetch SDL2 (MinGW)
        run: |
          wget https://github.com/libsdl-org/SDL/releases/download/release-2.28.5/SDL2-devel-2.28.5-mingw.tar.gz
          tar xzf SDL2-devel-2.28.5-mingw.tar.gz
          mv SDL2-2.28.5/x86_64-w64-mingw32 sdl2_win
      - name: 🔨 Forge Source Code
        run: python scripts/phase_ignition.py
      - name: 📜 Forge The Construct
        run: python scripts/construct_builder.py
      - name: 🪟 Compile Windows
        run: |
          INC="-Ipackages/common -Ipackages/simulation -Ipackages/protocol -Ipackages/map -Isdl2_win/include/SDL2 -Dmain=SDL_main"
          LIB="-Lsdl2_win/lib -lmingw32 -lSDL2main -lSDL2 -lopengl32 -lglu32 -lm"
          SRCS="apps/shank-fps/src/main.c packages/common/physics.c packages/map/map.c"
          x86_64-w64-mingw32-gcc -o shank_pit.exe $SRCS $INC $LIB
          cp sdl2_win/bin/SDL2.dll .
      - name: 🐧 Compile Linux
        run: |
          INC="-Ipackages/common -Ipackages/simulation -Ipackages/protocol -Ipackages/map"
          LIB="$(sdl2-config --cflags --libs) -lGL -lGLU -lm"
          SRCS="apps/shank-fps/src/main.c packages/common/physics.c packages/map/map.c"
          gcc -o shank_pit.bin $SRCS $INC $LIB
      - name: 📦 Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: SKATEBOARD_GEOMETRY_BUILD
          path: |
            *.exe
            *.bin
            SDL2.dll
            SKATEBOARD_CONSTRUCT.txt
